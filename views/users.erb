<section id="users-stuff" class="stuff">
  <div class="container">
    <h1>Users</h1>
  </div>
</section>

<section id="users-content">
  <div class="container">
    <% @users.each do |user| %>
      <p class="user-link">
        <a href="/user/<%= user.id %>">
          <% if user.nickname %><%= user.nickname %><% else %><%= user.user_name %><% end %>
        </a>
      </p>
    <% end %>
  </div>
</section>
